<div id="slideshow" class="slideshow">
    {{#foreach posts  from="0" to="2"}}
        {{#if img_url}}
            <div class="slide query" >
                <div class="bg-overlay" style="background-image:url({{img_url}})"></div>
                <blockquote>
                    <p>{{{title}}}</p>
                    <a class="view-details noSwipe" href="{{url}}">
                      阅读详情 <svg style="margin-top:2px;" class="icon icon-fast-forward"><use xlink:href="#icon-fast-forward"></use></svg>
                    </a>
                </blockquote>
            </div>
        {{/if}}
    {{/foreach}}
</div>